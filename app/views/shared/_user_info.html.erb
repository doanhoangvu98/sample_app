<%= link_to gravatar_for(current_user, size: Settings.shared.userinfo.size),
  current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("shared.user.profile"), current_user %></span>
<span><%= t("shared.user.micropost", count: current_user.microposts.size) %>
</span>
